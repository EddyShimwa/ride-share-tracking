// src/main.jsx or wherever you're using the react-pro-sidebar components
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import './index.css' // Import the CSS file here

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);